import makeFilepath from"./makeFilepath.mjs";import fileExists from"./fileExists.mjs";import{readFileSync}from"fs";import importRegex from"./importRegex.mjs";export const isCommonModule=e=>{const t=makeFilepath(e.path,"package.json");if(fileExists(t)){const e=JSON.parse(readFileSync(t).toString());if(e.type&&"module"===e.type)return!1}const i=readFileSync(e.file).toString();return/require\(['`"].+['`"]\)/.test(i)&&!importRegex().test(i)};export default isCommonModule;